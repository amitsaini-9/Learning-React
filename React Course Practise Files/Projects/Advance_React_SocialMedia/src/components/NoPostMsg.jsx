import Styles from "./NoPostMsg.module.css";
const NoPostMsg = () => {
  return (
    <center>
      <h1 className={`${Styles.noPost}`}>There is No Post Currently</h1>
    </center>
  );
};
export default NoPostMsg;
